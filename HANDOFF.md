# 프로젝트 핸드오프 노트

세션 이동 시 컨텍스트 빠르게 잡기 위한 문서. 사용법/구조는 `README.md` 참고,
이 문서는 "왜 이렇게 됐고 뭐가 아직 문제인지"에 집중.

## 목표

저장된 CCTV(어안렌즈, 천장 설치, 640x640) 주차장 사진을 넣으면, 사람 개입
없이(사진 넣기/결과 확인만) 각 주차 슬롯에 라벨(색깔 있는 폴리곤 테두리,
채우기 없음)을 자동으로 씌워서 저장하는 배치 처리 프로그램.

핵심 제약(사용자가 반복 강조):
- 슬롯 좌표, 카메라별 마스크 등 **사람이 카메라마다 손으로 입력하는 건 전부 금지**.
  사람이 하는 일은 "사진 넣기 / 결과(라벨된 사진) 다운로드"뿐.
- 라벨 테두리는 **슬롯의 실제 흰 선을 절대 못 나감**.
- 딥러닝 쓸 거면 **완전 로컬/오프라인만** — CCTV 사진을 외부 API로 못 보냄.
- "앞유리"는 실제 이미지에서 탐지할 대상이 아니라, 차가 정상 주차했다고
  가정했을 때 그 앞유리 위치를 라벨이 절반(2/4) 이상 덮도록 라벨을
  **위치/크기 잡는 기준**일 뿐 — 이번 세션에 명확히 정정됨.

## 현재 파이프라인 (구현/테스트 완료, 실제 코드)

1. `src/slot_detection.py` — 카메라 원본 프레임 폴더 → 슬롯 폴리곤 자동 탐지
   - median 스택(프레임 전체 픽셀 median)으로 차/움직이는 반사 제거, 고정된
     바닥 흰 선만 남김
   - 슬롯별 라벨 렌더링용으로 이미 있던 `LocalView`(로컬 gnomonic 왜곡보정)를
     격자로 재사용 — 어안렌즈에서 카메라 중심 안 지나는 실제 직선이 원본
     픽셀에서 곡선으로 찍히는 문제를 국소 패치 단위로 우회. 각 패치에서
     Canny+Hough로 직선 찾고 원본 좌표로 역변환
   - 연결영역 분리 → 면적/직사각형도 필터 → 노란/주황 사선(경고구역) 색상
     제외 → 폴리곤 각 꼭짓점을 안쪽으로 살짝 당김(인셋, closing이 부풀린 만큼
     보정해서 실제 흰 선 안에 들어오게)
2. `src/generate_config.py` — 위를 감싸는 CLI. 카메라 프레임 폴더 → config
   JSON(`config/<camera_id>.json`) 자동 생성. 저신뢰/0개 탐지면
   `needs_review=True` 반환
3. `src/pipeline.py`의 `run_auto`/`run_auto_all` — 라벨 위치는 **고정 규칙**
   (슬롯 정규화 좌표 (0.5,0.5) 중심, 슬롯 크기의 55%) — 실제 차/앞유리 CV
   탐지 전혀 안 함. 오늘 세션에서 옛날 `windshield.py`/`candidate.py`(그림자를
   앞유리로 오인식해서 빈 프레임에도 가짜 라벨 씌우던 버그) 대신 이걸로 교체함
4. `src/batch_processor.py` — 카메라 폴더 전체 일괄 처리, output/review 분류

`windshield.py`/`candidate.py`는 이제 pipeline에서 안 쓰임(고아 코드, 자기
테스트는 아직 통과하지만 실제로 안 불림) — 다음 세션에서 완전히 지워도 됨.

23개 카메라(`no_label/P1_B1_1_1`~`24`, 20 없음) 전부 config 자동 생성 +
첫 프레임 라벨링 검증 완료(`output/all_cameras/*.png`).

## 미해결 핵심 문제: 슬롯 vs 슬롯 아닌 바닥 표시 오탐

`slot_detection.py`가 기하/색상 특징(면적, 직사각형도, 색상)만으로 판단하다
보니, 진짜 주차 슬롯이 아닌 다른 바닥 표시(차선 방향 화살표, 볼라드 경고
구역, 횡단보도 등)도 모양이 비슷해서 슬롯으로 오탐하는 경우가 남아있음.
이건 "이게 뭔지 이해해야 하는" 내용 판별 문제라 순수 기하/색상으로는
원천적 한계가 있다는 게 이번 세션에 충분히 확인됨.

### 이번 세션에 시도하고 실패/한계 확인된 것들 (전부 실제 코드로 검증, 추측 아님)

고전 CV (전부 `no_label/P1_B1_1_9`, `_8`, `_21` 실제 데이터로 테스트):
1. 단순 밝기 임계값 + 윤곽선 — 반사광 노이즈에 압도됨
2. 면적/직사각형도 필터만 — 배경 선반 구조도 통과함
3. HSV 색상(노란 경고선) 필터 — 경고구역은 잡지만 화살표/볼라드는 못 잡음
4. 텍스처 분산(local std) 기반 바닥 마스크 — 배경 선반은 잘 걸러지는데
   각 슬롯 자체가 자기 경계선 때문에 flood-fill 연결성이 끊겨서 실제
   슬롯도 같이 걸러짐 (trade-off, 카메라마다 다르게 깨짐)
5. 지면 평면(z=H) 3D 재투영으로 실세계 직사각형 여부 검증 — 카메라 주변부가
   시야각 90도에 가까워서 수치적으로 터짐(관측 거리/각도 발산)
6. BEV(전체 화면 탑다운 워핑) — 배경(선반/간판)은 확실히 뭉개져서 잘
   걸러지는데, 화살표/볼라드처럼 **진짜 바닥 위에 있는** 표시는 기하학적으로
   슬롯이랑 구분이 안 됨(둘 다 바닥 평면 위의 진짜 직사각형이라 당연함)
7. adaptive threshold + morphology(빛 반사 줄이기 시도, 사용자 제안) —
   반사 노이즈는 줄지만 세게 하면 실제 슬롯 경계선도 같이 지워짐. 약하게
   하면 반사 노이즈 그대로 남음. 완벽한 중간값 못 찾음

로컬 딥러닝:
8. CLIP(open_clip, ViT-B-32, openai 가중치) 제로샷 분류 — 후보 박스 크롭을
   "빈 주차칸" vs "차선 화살표" vs "배경 선반" 등 텍스트 프롬프트랑 비교.
   유사도 값이 0.18~0.28로 사실상 무작위 수준, 실제 슬롯 8개 중 1개만 맞힘.
   원인: CLIP은 일반 인터넷 사진(눈높이 시점) 학습이라 이 천장 어안렌즈
   탑다운 시점은 학습 분포에 아예 없음 — 이 프로젝트 초반에 COCO 학습
   YOLO가 이 카메라 각도에서 차량을 하나도 못 찾았던 것과 정확히 같은
   원인/패턴 (도메인 미스매치)

검토했지만 시도 안 한 것 (사용자가 마지막에 제안, 다음 세션 후보):
9. PKLot/LaneNet 등 "이미 학습된 주차선 전용 모델" — 검토 결과 둘 다 부적합.
   PKLot은 선 segmentation 데이터셋이 아니라 이미 잘린 슬롯의 점유
   분류(occupancy classification) 데이터셋이고 촬영 각도도 건물 위층에서
   비스듬히 내려다본 시점(oblique). LaneNet은 고속도로 전방 대시캠 시점
   차선 검출용. 둘 다 이 천장 어안렌즈 정수리뷰랑 시점이 완전히 다름 —
   8번(CLIP)이랑 똑같은 도메인 미스매치 패턴 반복될 가능성 높음(아직 실제
   코드로 검증은 안 했음, 이론적 판단만 내림).

### 이어진 세션(같은 날, 후속)에 시도/확인된 것들

**적용/유지됨:**
- **A. grayscale+saturation Canny union** (`slot_detection.py`의
  `_build_edge_mask`) — 라인 색이 바닥이랑 채도는 다른데 명도(grayscale)는
  비슷해서 기존 grayscale 전용 Canny가 못 잡던 미탐 슬롯을 잡음(색 다른
  슬롯 문제, 사용자가 직접 관찰해서 지적). saturation(HSV S채널) Canny를
  추가로 돌려서 grayscale Canny랑 union. `_9`/`_8`/`_21` 3개 카메라
  before/after 실제 이미지로 검증: `_8`의 미탐 슬롯 하나 새로 잡힘,
  `_21`도 미탐 슬롯 하나 새로 잡힘. 트레이드오프로 `_9`에 배경 벽 구조물
  오탐 하나 새로 생김(순개선이지만 공짜는 아님). **23개 카메라 전부
  재탐지해서 `output/all_cameras/*.png` 갱신 완료. `config/*.json`은 아직
  이 변경 반영 안 됨(재생성 필요).**

**시도했다가 실측 데이터로 반증되어 폐기:**
- **B. 후보 폴리곤 각도 군집 필터** (오탐은 이웃 없이 고립돼 있을 거라는
  가설) — `_21`번 카메라 실제 좌표로 계산해보니, 진짜 슬롯 2개(top row)의
  각도 간격이 41°인데 오탐(경고판) 각도 간격도 43°로 거의 동일. 고정
  임계값으론 진짜/가짜 구분 불가 확인, 구현 안 함.

**시도했다가 이 해상도에서 신호가 너무 약해서 폐기 (5가지 파라미터 조합,
전부 실제 코드/이미지로 검증):**
- 슬롯 입구 라인에 작은 검은 점 3개(휠스탑/스터드로 추정)가 규칙적으로
  박혀있는 걸 육안으로 발견(3x~6x 확대 크롭으로 확인) — 실제 슬롯 3곳
  전부 있고, 오탐 위치(경고판/배경구조물/횡단보도) 3곳 전부 없음. 신호
  자체는 진짜였음.
  1. 전역 밝기-threshold 블롭 검출(`floor_estimate - gray > thresh`) →
     전체 이미지 기준 노이즈 130~250개(차선/횡단보도 가장자리
     anti-aliasing도 다 같이 잡힘)
  2. 후보 폴리곤 테두리에 겹치는 좁은 band로 범위 좁혀서 재검증 → 후보
     자신의 페인트 라인이 조각나면서 "점 2개"로 오인식돼 오탐 그대로 안
     걸러지고, 반대로 진짜 슬롯 라인이 안 조각나면 탈락(`_8`에서 진짜
     슬롯 2개 날아감)
  3. 검출된 edge_mask를 dilate해서 라인 픽셀 자체를 마스크에서 제외 →
     너무 세게 지워져서 거의 다 탈락(`_8`: 8→0)
  4. band를 라인에서 살짝 띄운 annulus(고리)로 교체 → 카메라마다 결과
     들쭉날쭉(`_9`는 오탐 그대로 유지, `_8`은 진짜 슬롯 3개 날아감,
     `_21`은 변화 없음) — 밝기 임계값 하나로 카메라 간 조명 차이를
     못 버팀
  5. HoughCircles로 전환(원형 매칭이 밝기 임계값보다 더 구체적일 거라
     기대) → 파라미터 세게 하면 실제/오탐 위치 전부 0개, 느슨하게 하면
     전부 1개 안팎 — 구분력 없음. 원인: 이 카메라 해상도에서 점 하나가
     실측 2~3px밖에 안 돼서 JPEG 압축/반사 노이즈랑 신호가 거의 안
     갈림(median 스택 후에도).
  → 결론: 아이디어(스터드가 슬롯의 물리적 증거)는 맞지만, 이 카메라
  해상도의 고전 CV로는 이 신호를 노이즈랑 안정적으로 못 가름. 관련 코드는
  전부 롤백(`_dark_stud_mask`/`_has_entrance_studs`/`require_studs` 파라미터
  다 제거, `slot_detection.py`는 A만 있는 상태로 복귀, 테스트 44개 통과
  확인됨). 다음에 재시도한다면: 슈퍼샘플링/서브픽셀 누적, 매치드 필터
  (템플릿 상관), 혹은 해상도를 더 확보할 다른 방법 필요 — 같은 방식
  파라미터만 또 바꾸는 건 이미 5번 해봐서 비추.

### 남은 현실적 선택지 (사용자에게 아직 결론 안 남)

- A. 지금 상태(A 적용된 기하 기반 최선 결과)로 확정하고, 애매한 카메라/
  슬롯은 `generate_config()`의 `needs_review` 플래그로 사람이 결과만 한 번
  훑어보게(좌표 입력 아님) — 이미 이 방향으로 진행 중, 다음 스텝은
  `config/*.json` 23개를 A 반영해서 재생성하는 것
- B. 이 카메라 시점 사진에 직접 소량 라벨링(예: 20~50장 "이게 슬롯이다/
  아니다")해서 가벼운 분류기 파인튜닝 — 정확도는 오르지만 "사람 입력 전혀
  없음" 원칙과 정면 충돌(한 번은 사람이 정답 표시 필요). 이전에 한 번
  물어봤고 A를 선택했었음
- C. PKLot/LaneNet 파인튜닝 시도(자체 라벨 없이 전이학습만) — 위 이유로
  실효성 의문, 실제로 검증은 안 해봄

## 파일 상태

- `config/*.json` — 23개 카메라 전부 `generate_config.py`로 실제 재생성됨
  (예전 손으로 찍은 임시값 아님)
- `src/slot_detection.py`, `src/generate_config.py` — 새로 만듦, 테스트 있음
- `src/pipeline.py` — `run_auto`/`run_auto_all` 고정 규칙으로 교체 완료
- `src/windshield.py`, `src/candidate.py` — 이제 아무데서도 안 불림(고아).
  자기 테스트(`test_windshield.py`, `test_candidate.py`)는 통과하지만 실제
  파이프라인에서 죽은 코드. 다음 세션에서 지울지 결정 필요
- 테스트 44개 전부 통과
- git: 아직 커밋 안 함 (`README.md`, `config/*.json`, 새 파일들 전부 uncommitted)
